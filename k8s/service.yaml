apiVersion: v1
kind: Service
metadata:
  name: trabalho-adrian-service
spec:
  selector:
    app: trabalho-adrian  ports:
  - protocol: TCP
    port: 3123
    targetPort: 3123
  type: NodePort
